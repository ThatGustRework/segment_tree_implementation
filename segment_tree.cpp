#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <queue>

using namespace std;

template <class T>
struct segment_tree
{
    T *root; /*Pointer to the root*/
    int srArraySize;
    int originalArraySize;
    segment_tree(vector<T>)
    {
    }
    ~segment_tree()
    {
    }
};

int main()
{
    return 0;
}